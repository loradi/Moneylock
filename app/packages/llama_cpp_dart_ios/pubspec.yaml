name: llama_cpp_dart_ios
description: Vendored Llama.xcframework (llama.cpp) for llama_cpp_dart on iOS.
version: 0.2.2
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=1.20.0'

flutter:
  plugin:
    platforms:
      ios:
        pluginClass: LlamaCppDartIosPlugin